# EE5332 Project — Bitonic Sort (HLS)

## Overview

Hardware implementation of Bitonic Sort using Vitis HLS.

## Simulation

```bash
make sim
```

Compiles the kernel and testbench, runs the binary, and prints `PASS` or `FAIL`.
